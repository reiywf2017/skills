请用 Java 写一个 UserDataImporter 类，实现以下功能：

  1. 从指定路径的 CSV 文件中读取用户数据（姓名、邮箱、手机号）
  2. 校验每行数据是否合法（字段非空、邮箱格式正确）
  3. 将合法用户批量插入 MySQL 数据库
  4. 生成随机邀请码并记录到日志
  5. 处理完成后，无论成功或失败，都要输出汇总信息
  6. 如果文件不存在或数据库连接失败，要有相应的异常处理
